# Permite ao usuário www-data (Nginx/PHP-FPM) executar comandos do iproute2 sem senha
# Necessário para aplicar configurações de rede da interface Web diretamente no Linux
www-data ALL=(ALL) NOPASSWD: /sbin/ip *, /usr/sbin/ip *
